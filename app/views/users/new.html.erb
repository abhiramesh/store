<h2>Tell us who you are!</h2>

<div class="container">
	<%= form_for(@user) do |f| %>

	  <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :email %>
      <%= f.text_field :email %>

<%= link_to 'Back', users_path %>

<% end %>
</div>
